<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="AnimBottom" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/epub_push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/epub_push_bottom_out</item>
    </style>

</resources>
